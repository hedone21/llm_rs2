---
description: Start the web dashboard dev server for benchmark visualization
---

// turbo-all

1. Activate the virtual environment and start Flask:
```bash
cd /home/go/Workspace/llm_rs2/web_dashboard && source venv/bin/activate && python app.py
```

2. Open the dashboard in browser:
```
http://localhost:5000
```
